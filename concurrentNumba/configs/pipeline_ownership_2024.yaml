base_config: ../../configs/pipeline_ownership_2024_base.yaml

runs:
  n_runs: 1

execution:
  max_workers: 8

phase1_clean:
  use_numba: true
  sampling:
    method: fast_hash
    stage: canonical

phase2_graph:
  use_numba: true

phase3_algos:
  use_numba: true

phase4_score:
  use_numba: true

